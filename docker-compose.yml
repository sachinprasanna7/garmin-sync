version: '3.8'

services:
  health-sync:
    build: .
    container_name: health_sync_runner
    profiles: ["cli"] 
    # These two lines are required for the MFA input() prompt to work
    stdin_open: true 
    tty: true
    volumes:
      - ./secrets:/app/secrets
    env_file:
      - ./secrets/.env